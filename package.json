{
   "name": "mm-google-task",
   "version": "1.0.0",
   "description": "MagicMirror module to display Google Tasks",
   "main": "mm-google-task.js",
   "scripts": {
      "test": "echo \"Error: no test specified\" && exit 1"
   },

  "dependencies": {
    "googleapis": "^71.0.0"
  }
}